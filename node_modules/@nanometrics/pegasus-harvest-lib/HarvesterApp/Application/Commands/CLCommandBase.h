#ifndef CLCommandBase_h__
#define CLCommandBase_h__

#include "../../ConsoleApp/ICLCommand.h"

#include <Hasher/IHasher.h>
#include <Logger/ILogger.h>

#include <string> // std::string

class CLCommandBase : public ICLCommand
{
public:
  CLCommandBase(const std::string& name, const std::shared_ptr<ILogger>& logger);
  virtual ~CLCommandBase() = default;
  
private:
  bool _parseHasher(const char* arg);
  bool _parseVerbosity(const char* arg);

protected:
  std::shared_ptr<IHasher> m_hasher;
  uint8_t m_verbosity;
};

#endif //CLCommandBase_h__

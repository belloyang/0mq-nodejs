#ifndef CLCommand_VolumeInfo_h__
#define CLCommand_VolumeInfo_h__

#include "CLCommandBase.h"

class CLCommand_VolumeInfo : public CLCommandBase
{
public:
  CLCommand_VolumeInfo(const std::shared_ptr<ILogger>& logger);
  virtual ~CLCommand_VolumeInfo() = default;

private:
  virtual int _runImpl() __OVERRIDES__(ICLCommand);
  virtual std::string _getDescriptionImpl() const __OVERRIDES__(ICLCommand);
  
  bool _parseLibPath(const char* arg);
  bool _parseVolumeId(const char* arg);
  bool _parseLoadSections(const char* arg);
  
private:
  std::string m_libPath;
  uint32_t m_volumeId;
  uint32_t m_nSectionsToLoad;
};

#endif //CLCommand_VolumeInfo_h__

#ifndef CLCommand_GenerateLogs_h__
#define CLCommand_GenerateLogs_h__

#include "CLCommandBase.h"

class CLCommand_GenerateLogs : public CLCommandBase
{
public:
  CLCommand_GenerateLogs(const std::shared_ptr<ILogger>& logger);
  virtual ~CLCommand_GenerateLogs() = default;

private:
  virtual int _runImpl() __OVERRIDES__(ICLCommand);
  virtual std::string _getDescriptionImpl() const __OVERRIDES__(ICLCommand);
  
  bool _parseLibPath(const char* arg);
  bool _parseLogsCount(const char* arg);
  bool _parseStartTimestamp(const char* arg);
  
private:
  std::string m_libPath;
  int32_t m_nLogs;
  timestamp_t m_startTimestamp;
};

#endif //CLCommand_GenerateLogs_h__

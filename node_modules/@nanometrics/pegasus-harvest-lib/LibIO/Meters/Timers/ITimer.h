#ifndef ITimer_h__
#define ITimer_h__

#include "../../libio_defines.h"
#include <string.h>

class ITimer
{
protected:
  ITimer() {}
  
public:
  virtual ~ITimer() {}
  virtual timestamp_t now() const = 0;
  virtual timediff_t getElapsed(const timestamp_t since) const = 0;
};

#endif //ITimer_h__

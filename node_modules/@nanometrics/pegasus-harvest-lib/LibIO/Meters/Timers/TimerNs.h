#ifndef TimerNs_h__
#define TimerNs_h__

#include "ITimer.h"

class TimerNs : public ITimer
{
public:
  TimerNs();
  virtual ~TimerNs() = default;
  
  virtual timestamp_t now() const __OVERRIDES__(ITimer);
  virtual timediff_t getElapsed(const timestamp_t point) const __OVERRIDES__(ITimer);
};

#endif //TimerMs_h__

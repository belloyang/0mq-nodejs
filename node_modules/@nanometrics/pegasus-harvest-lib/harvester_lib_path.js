"use strict";
exports.__esModule = true;
var path = require("path");
var harvesterLibName = 'harvester';
var harvesterLibPath = 'build/Release/';
var SharedLibSuffixMap = {
    win32: '.dll',
    darwin: '.dylib',
    linux: '.so'
};
if (!SharedLibSuffixMap[process.platform]) {
    console.error('Unsupported platform', process.platform, SharedLibSuffixMap[process.platform], SharedLibSuffixMap);
}
exports.HarvesterLibPath = path.join(__dirname, harvesterLibPath + harvesterLibName + SharedLibSuffixMap[process.platform]);

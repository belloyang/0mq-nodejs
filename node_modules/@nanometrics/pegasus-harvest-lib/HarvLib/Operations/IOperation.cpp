#include "IOperation.h"

#include <Logger/NullLogger.h>

IOperation::IOperation(const std::string& name)
: m_name(name)
{
  __DEV_CALLSTACK_FUNC__;
}

bool IOperation::execute(const std::shared_ptr<IHasher>& hasher,
    const std::shared_ptr<ILogger>& logger)
{
  __DEV_CALLSTACK_FUNC__;
  return this->_executeImpl(hasher, logger);
}

bool IOperation::stop()
{
  __DEV_CALLSTACK_FUNC__;
  return this->_stopImpl();
}

#ifndef IProcessorDelegate_h__
#define IProcessorDelegate_h__

#include "../../harvlib_defines.h"

#include <Library/PSFDataElement.h>

class IProcessorDelegate
{
public:
  virtual ~IProcessorDelegate() = default;

  virtual void onPSFRead(const PSFDataElement* dataElement) = 0;
  virtual void onCompleted(const eOperationStatus status) = 0;

protected:
  IProcessorDelegate() = default;
};

#endif //IProcessorDelegate_h__

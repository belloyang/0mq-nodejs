#ifndef IDataDesc_h__
#define IDataDesc_h__

#include "../../harvlib_defines.h"

/**
 * Basic IDataDesc - basic class for various data used within filters
 */
class IDataDesc
{
public:
  typedef enum : uint32_t {
    DATA_TYPE_PSF = 0, /*!< PSF data */
    DATA_TYPE_PDB, /*!< PDB data */
    DATA_TYPE_ERROR, /*!< error info */
    DATA_TYPE_PROGRESS, /*!< progress info */
    DATA_TYPE_COUNT
  } eType;

protected:
  IDataDesc(const eType type) : m_type(type) {}

public:
  virtual ~IDataDesc(){}

  /**
   * Data type getter
   * @return data type that object represents
   */
  FORCE_INLINE eType getType() const { return m_type; }

protected:
  const eType m_type;
};

typedef IDataDesc* DataDescPtr;

#endif //IDataDesc_h__

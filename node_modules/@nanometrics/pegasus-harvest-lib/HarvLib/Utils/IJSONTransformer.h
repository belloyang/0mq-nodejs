#ifndef IJSONTransformer_h__
#define IJSONTransformer_h__

#include "../harvlib_defines.h"

class IJSONTransformer
{
public:
  virtual ~IJSONTransformer() = default;
  virtual timestamp_t timestamp(const timestamp_t& timestamp) const = 0;

protected:
  IJSONTransformer() = default;
};

#endif // IJSONTransformer_h__

#ifndef harvester_api_defines_h__
#define harvester_api_defines_h__

#include "../shared_defines.h"

#include <libio_defines.h>
#include <psflib_defines.h>
#include <pdblib_defines.h>
#include <harvlib_defines.h>

#define EXTERN_C extern "C"

#if defined(DEV_PLATFORM_WIN32)
  #define DLL_EXPORT __declspec(dllexport)
#else
  #define DLL_EXPORT
#endif

#define EXPORT_FUNC EXTERN_C DLL_EXPORT

typedef int64_t nodejs_time_t; //timestamp used for Node.js (microseconds, used to fit into JavaScript MAX_SAFE_INTEGER)

typedef enum : uint32_t
{
  HARVAPI_ERROR_CODE_BASE = 0x50000000, /*! base error code for Harvester API library */

  HARVAPI_ERROR_CODE_BASE_OPERATION = HARVAPI_ERROR_CODE_BASE + 0x00001000, /*! base error code for Harvester API operations */
  HARVAPI_ERROR_OPERATION_FAILED = HARVAPI_ERROR_CODE_BASE_OPERATION + 0x00000001, /*!< operation failed */
  HARVAPI_ERROR_INVALID_ARGUMENT = HARVAPI_ERROR_CODE_BASE_OPERATION + 0x00000002, /*!< invalid argument */

} eHarvAPIErrorCode;

#endif //harvester_api_defines_h__

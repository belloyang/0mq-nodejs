#ifndef nodejs_utils_h__
#define nodejs_utils_h__

#include "harvester_api_defines.h"

#include <Utils/pdb_utils.h>

namespace nodejs_utils
{
  FORCE_INLINE timestamp_t nodejstime_to_timestamp(const nodejs_time_t time)
  {
    __DEV_CALLSTACK_FUNC__;
    return pdb_utils::microseconds_to_nanoseconds(time);
  }

  FORCE_INLINE nodejs_time_t timestamp_to_nodejstime(const timestamp_t timestamp)
  {
    __DEV_CALLSTACK_FUNC__;
    // nanoseconds to microseconds.
    // do not use pdb_utils::nanoseconds_to_microseconds because of rounding up
    return static_cast<nodejs_time_t>(timestamp / 1000);
  }
}

#endif //nodejs_utils_h__

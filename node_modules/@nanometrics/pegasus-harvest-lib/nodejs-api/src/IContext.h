#ifndef IContext_h__
#define IContext_h__

#include <DataTypes/HarvestLog/MsgHarvestLog.h>

#include <vector>

class IContext
{
public:
  virtual uint32_t fetchHistory(std::vector<MsgHarvestLog::Header>& output) = 0;
  virtual void pushHistory(const MsgHarvestLog::Header& history) = 0;

protected:
  IContext() {};
  virtual ~IContext() {};
};

#endif //IAsyncTask_h__

#ifndef AsyncTask_GetVolumeInfo_h__
#define AsyncTask_GetVolumeInfo_h__

#include "IAsyncTask.h"
#include <Operations/operations.h>

class AsyncTask_GetVolumeInfo : public IAsyncTask
{
public:
  AsyncTask_GetVolumeInfo(const std::string& libPath, const uint32_t volumeId, 
    const std::shared_ptr<IHasher>& hasher, const std::shared_ptr<ILogger>& logger);
  virtual ~AsyncTask_GetVolumeInfo() = default;

private:
  virtual void _runImpl(const std::shared_ptr<IContext>& context) __OVERRIDES__(IAsyncTask);
  virtual bool _stopImpl() __OVERRIDES__(IAsyncTask);

private:
  const std::string m_libPath;
  const uint32_t m_volumeId;
  std::shared_ptr<IOperation> m_operation;
};

#endif //AsyncTask_GetVolumeInfo_h__
